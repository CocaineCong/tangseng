# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

config/config.yml
app/favorite/config/config.yml
app/search-engine/config/config.yml
app/user/config/config.yml
.idea
app/user/logs
app/gateway/logs
app/favorite/logs
app/search-engine/logs
app/search-engine/internal/data
logs
data
source